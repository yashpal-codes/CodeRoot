---
id: Flexbox
title: Flexbox
sidebar_position: 4
description: All about Flexbox and it's magic
---

### Flexbox is CSS display property

- Flexbox is directional base layout, which has two axis/direction

  - Main axis
  - Cross axis

- In CSS we define them by flex direction,
  - If flex-direction is row, than Main axis will be horizontal and cross axis will be vertical
  - If flex-direction is column, than Main axis will be vertical and cross axis will be horizontal.

<img
  alt="flexbox"
  src={require("./assets/flexbox.png").default}
  style={{ margin: "auto", display: "block" }}
/>

<br />

- Flex is container based, there is parent container and flex-items are child.
- Both FlexBox Container and Flex Items have properties.

### Flexbox Container Properties

#### 1. **`display:flex`**

#### 2. **`flex-direction`** : Define flex Main and Cross Axis

    - row
    - column
    - row-reverse
    - column-reverse

#### 3. **`flex-wrap`** : Define if flex items will wrap or not

    - wrap : when no enough space available, last items will be moved to next row/line.
    - Consider this you are writing in a line into text book, while writing you reach at the end of line, you will start writing to next line below current one.
    - Similar way in wrap, when wrap is enabled, and there is no enough space, last few items will move to next row.
    - no-wrap : items does not float. This is default.
    - wrap-reverse : reverse the wrap.

<img
  alt="flexbox"
  src={require("./assets/flex-wrap.png").default}
  style={{ margin: "auto", display: "block" }}
/>

#### 4. **`flex-flow`** : Shorthand property for flex direction and wrap

    - column wrap
    - row no-wrapper

#### 5. **`justify-content`** : Most of properties start with 'justify' are aligned with Main axis

    - This property moved flex items on Main axis, if Main axis is horizontal, items will be moved horizontally,
    - If Main axis is vertical, items will be moved vertically.
    - flex-start
    - center
    - flex-end
    - space-between

<img
  alt="flexbox"
  src={require("./assets/flex-justify.png").default}
  style={{ margin: "auto", display: "block" }}
/>

<br />

#### 6. **`align-items`** : Properties start with 'align' are aligned with Cross Axis

    - This property moved flex items on Cross axis, eg: for flex row, cross axis will be vertically, so items will be moved on vertical line.
    - Align Items only works if heigh is fixed or defined.
    - flex-start
    - center: this align whole items to the center of container
    - flex-end
    - stretch
    - baseline : this align text items to the center.

<img
  alt="flexbox"
  src={require("./assets/flex-align-items.png").default}
  style={{ margin: "auto", display: "block" }}
/>

<br />

#### 7. **`align-contents`** :

    - With align-items, the items align relative to their own row, not the whole container.
      So if there is only one row, it looks centered in the container.
      But if there are multiple rows, each row aligns individually, not as one group.
    - With align-content, the entire group of rows is aligned inside the container (centered, spaced, stretched, etc.), not just individual items.
    -  Unlike `align-items`, which aligns **items inside each row**, `align-content` aligns the **rows themselves**.
    - If align the flex-container lines.
    - It only works with Multi line flex container with ref to Cross Axis.
    - Only works with flex-wrap and wrap-reverse.
    - [**See this CodePen example**](https://codepen.io/Yashpal-Singh-Rathod/pen/pvyKWdP)

#### 8. **`gap`** : Gap define space between items

    - The gap property explicitly controls the space between flex items

    ``` css
      .container {
        display: flex;
        ...
        gap: 10px; /* both row-gap and column-gap are 10 */
        gap: 10px 20px; /* row-gap column gap */
        row-gap: 10px;
        column-gap: 20px;
      }

    ```

### Flex Items Properties

#### 1. **`align-self`** :

    - Align self make an items to be placed on cross axis,
    - It supports, center, flex start, flex-end, baseline.
    - ** There is no Justify Self** as that is not possible technically,
    - In justify, items are moved across main axis, but they are in one line, so single items can not be moved individually.
    - Think like you are stuck in a traffic, there are cars in front and back side of you, so you can not individually move in that line.
    - While In cross axis, it is like three people climbing mountain suing rope, so any individual can move to top, center or bottom of mountain.

#### 2. **`flex-grow`** :

    - Given flex item will grow **Number** times bigger than other items in container when space available.
    - Default is 0.
    - The question is what is mean by **grow** here ?
      - Does item will grow in both height and width ?
      - No, in flexbox everything is related to axis, so grow/shrink also happen in relation with axis.
      - For flex:row, when grow is applied item **width** will increase,
      - For flex:column, when grow is applied item **height** will increase.
      - Same applied to shrink, so grow and shrink applies to Main axis.

### Interactive Flex Grow & Shrink Demo

```jsx live
function FlexGrowShrinkDemo() {
  const [flexDirection, setFlexDirection] = useState("row");
  const [growValues, setGrowValues] = useState([1, 0, 0]);
  const [shrinkValues, setShrinkValues] = useState([1, 1, 1]);
  const [containerSize, setContainerSize] = useState(100);

  const colors = ["#FF6B6B", "#4ECDC4", "#45B7D1"];
  const labels = ["Item 1 :", "Item 2 :", "Item 3 :"];
  const sizeOptions = [20, 40, 60, 80, 100];

  const sizeLabel = flexDirection === "row" ? "Width" : "Height";
  const [flexBasisValues, setFlexBasisValues] = useState([
    "auto",
    "auto",
    "auto",
  ]);
  const basisOptions = [
    "Basis: auto",
    "Basis: 0px",
    "Basis: 50px",
    "Basis: 100px",
    "Basis: 200px",
  ];
  const toggleGrow = (index) => {
    const newGrowValues = [...growValues];
    newGrowValues[index] = newGrowValues[index] === 0 ? 1 : 0;
    setGrowValues(newGrowValues);
  };

  const toggleShrink = (index) => {
    const newShrinkValues = [...shrinkValues];
    newShrinkValues[index] = newShrinkValues[index] === 0 ? 1 : 0;
    setShrinkValues(newShrinkValues);
  };

  const getAxisLabel = () => {
    if (flexDirection === "row") {
      return { main: "Main Axis →", cross: "Cross Axis ↓" };
    } else {
      return { main: "Main Axis ↓", cross: "Cross Axis →" };
    }
  };

  const axisLabels = getAxisLabel();

  return (
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        gap: "20px",
        padding: "12px",
        background: "#1a1a1a",
        borderRadius: "12px",
        color: "#fff",
      }}
    >
      {/* Header */}
      <div style={{ textAlign: "center" }}>
        <p style={{ margin: "0", fontSize: "16px", color: "#e7dbdbff" }}>
          Toggle grow/shrink for each item and change flex direction
        </p>
      </div>

      {/* Controls */}
      <div
        style={{
          display: "flex",
          gap: "12px",
          justifyContent: "center",
          flexWrap: "wrap",
          alignItems: "center",
        }}
      >
        <select
          value={flexDirection}
          onChange={(e) => setFlexDirection(e.target.value)}
          style={{
            padding: "8px 12px",
            background: "#2e3f49ff",
            color: "white",
            border: "none",
            borderRadius: "6px",
            cursor: "pointer",
            fontSize: "13px",
            fontWeight: "600",
          }}
        >
          <option value="row">Direction: Row →</option>
          <option value="column">Direction: Column ↓</option>
        </select>

        <select
          value={containerSize}
          onChange={(e) => setContainerSize(parseInt(e.target.value))}
          style={{
            padding: "8px 12px",
            background: "#2e3f49ff",
            color: "white",
            border: "none",
            borderRadius: "6px",
            cursor: "pointer",
            fontSize: "13px",
            fontWeight: "600",
          }}
        >
          {sizeOptions.map((size) => (
            <option key={size} value={size}>
              {sizeLabel}: {size}%
            </option>
          ))}
        </select>
      </div>

      {/* Axis Labels */}
      <div
        style={{
          display: "flex",
          justifyContent: "center",
          gap: "15px",
          algnItems: "center",
          textAlign: "center",
          fontSize: "14px",
          color: "#8dd7eeff",
          fontWeight: "600",
        }}
      >
        <div>{axisLabels.main}</div>
        <div>{axisLabels.cross}</div>
      </div>

      {/* Flex Container (wrapped inside fixed-height demo area) */}
      <div
        style={{
          width: "100%",
          maxWidth: "900px",
          height:
            flexDirection === "column"
              ? "360px"
              : "auto" /* fixed demo height only for column mode */,
          margin: "0 auto",
          display: "flex",
          alignItems: flexDirection === "column" ? "center" : "stretch",
          justifyContent: "center",
          background: "transparent",
        }}
      >
        <div
          style={{
            display: "flex",
            flexDirection: flexDirection,
            /* when in row mode use percent width, when column mode use percent height of this fixed wrapper */
            width: flexDirection === "row" ? `${containerSize}%` : "100%",
            /* static row height so demo container isn't too small */
            height: flexDirection === "column" ? `${containerSize}%` : "120px",
            background: "#000",
            border: "3px solid #555",
            borderRadius: "8px",
            padding: "12px",
            gap: "8px",
            boxSizing: "border-box",
            overflow: "auto",
            transition: "all 0.3s ease",
            alignItems: "stretch",
          }}
        >
          {labels.map((label, index) => (
            <div
              key={index}
              style={{
                flex: `${growValues[index]} ${shrinkValues[index]} ${flexBasisValues[index]}`,
                background: colors[index],
                borderRadius: "6px",
                display: "flex",
                alignItems: "center",
                justifyContent: "center",
                fontSize: "12px",
                fontWeight: "600",
                color: "#000",
                minWidth: "40px",
                minHeight: "40px",
                transition: "flex 0.3s ease",
                textAlign: "center",
                padding: "8px",
              }}
            >
              {label}
            </div>
          ))}
        </div>
      </div>

      {/* Item Controls (label + two narrow buttons in one row) */}
      <div
        style={{
          display: "flex",
          flexDirection: "column",
          gap: "10px",
          width: "100%",
          maxWidth: "700px",
          margin: "0 auto",
        }}
      >
        {labels.map((label, index) => (
          <div
            key={index}
            style={{
              background: "#222",
              padding: "10px 12px",
              borderRadius: "8px",
              border: "2px solid #635f5fff",
              display: "flex",
              alignItems: "center",
              justifyContent: "flex-start",
              gap: "10px",
            }}
          >
            <div
              style={{
                fontSize: "14px",
                fontWeight: "600",
                color: "#fff",
                flex: "0 0 auto",
              }}
            >
              {label}
            </div>

            <div
              style={{
                display: "flex",
                gap: "6px",
                alignItems: "center",
              }}
            >
              <button
                onClick={() => toggleGrow(index)}
                style={{
                  padding: "6px",
                  background: growValues[index] === 1 ? "#4CAF50" : "#666",
                  color: "white",
                  border: "none",
                  borderRadius: "6px",
                  cursor: "pointer",
                  fontSize: "12px",
                  fontWeight: "600",
                  transition: "background 0.15s",
                  textAlign: "center",
                }}
              >
                Grow: {growValues[index]}
              </button>

              <button
                onClick={() => toggleShrink(index)}
                style={{
                  padding: "6px",
                  background: shrinkValues[index] === 1 ? "#4CAF50" : "#666",
                  color: "white",
                  border: "none",
                  borderRadius: "6px",
                  cursor: "pointer",
                  fontSize: "12px",
                  fontWeight: "600",
                  transition: "background 0.15s",
                  textAlign: "center",
                }}
              >
                Shrink: {shrinkValues[index]}
              </button>
              <select
                value={flexBasisValues[index]}
                onChange={(e) => {
                  const v = e.target.value;
                  const next = [...flexBasisValues];
                  next[index] = v;
                  setFlexBasisValues(next);
                }}
                style={{
                  padding: "6px 6px",
                  background: "#333",
                  color: "white",
                  border: "none",
                  borderRadius: "6px",
                  cursor: "pointer",
                  fontSize: "12px",
                  fontWeight: "600",
                }}
              >
                {basisOptions.map((b) => (
                  <option key={b} value={b}>
                    {b}
                  </option>
                ))}
              </select>
            </div>
          </div>
        ))}
      </div>

      {/* Info Box */}
      <div
        style={{
          background: "#222",
          padding: "12px",
          borderRadius: "8px",
          fontSize: "14px",
          lineHeight: "1.6",
          color: "#ddd",
        }}
      >
        <div
          style={{ fontWeight: "600", marginBottom: "10px", color: "#4ECDC4" }}
        >
          How it works:
        </div>
        <ul style={{ margin: "0", paddingLeft: "20px" }}>
          <li>
            <strong>Grow</strong>: Item expands to fill available space on the
            main axis
          </li>
          <li>
            <strong>Shrink</strong>: Item can shrink when space is limited
          </li>
          <li>Try reducing container width to see shrink in action</li>
        </ul>
      </div>
    </div>
  );
}
```

#### 3. **`flex-shrink`** :

    - Same as flex-grow but shrink by given number.
    - default is 1.

#### 4. **`flex-basis`** :

    - Defies initial length for flex-items
    - This defines the default size of an element before the remaining space is distributed
    - Default is **auto**
    - In auto extra space is distributes to new items.
    - flex-basis: 0px, means it take only space for context require and all other space is distributed.
    - In a Flex row, flex-basis does the same thing as width. In a Flex column, flex-basis does the same thing as height.

#### 5. **`flex`** :

    - This is shorthand for **flex-grow,flex-shrink,flex-basis**.
    - flex:0,0,0.5 means, grow is default, shrink is set to 0 and flex basis is 0.5.
    - [**Detailed Guide on Flex here **](https://www.joshwcomeau.com/css/interactive-guide-to-flexbox/)

#### 6. **`order`** :

    - By default teh flex items are laid out in the source order, but with help of **order** we can change the order.

### Flex values:

- `flex: 0 1 auto`

  - `flex-grow: 0` → Item **will not grow** even if there is free space.
  - `flex-shrink: 1` → Item **can shrink** when there isn’t enough space.
  - `flex-basis: auto` → Width is based on the item’s content.
  - The item is **shrinkable but not expandable**.

---

- `flex: 1 1 auto`

  - `flex-grow: 1` → Item can **grow** if there is extra space.
  - `flex-shrink: 1` → Item can **shrink** if needed.
  - `flex-basis: auto`.
  - Fully flexible: items grow and shrink based on available space and distribute leftover space **equally**.

---

- `flex: 0 0 auto`

  - `flex-grow: 0`
  - `flex-shrink: 0`
  - `flex-basis: auto`
  - The item remains **exactly its content size** — fully inflexible.

---

- `flex-basis: 0` vs `flex-basis: auto`

  - `flex-basis: 0`
  - Items start as width **0**
  - All available space is distributed according to `flex-grow` values

- `flex-basis: auto`

  - Items first take **their natural content width**
  - Only **remaining free space** is distributed based on `flex-grow`
  - Better when items have different content sizes.
